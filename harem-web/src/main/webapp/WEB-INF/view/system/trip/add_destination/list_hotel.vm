<script src="$utilWebconfig.getStaticResourcesPath()/resources/js/layerModal.js" type="text/javascript"></script>
    <form class="form-signin" id="hotelDOListForm" name="hotelDOListForm" action="$!utilWebconfig.getActionDefaultFontPath()/B2C/hotelManage/selectList" method="GET" role="form">

		<div class="tableGroup whole">
            <table class="table table-hover">
                <tr>
                    <th><input class="allChecked" name="allChecked" type="checkbox">全选 </th>
                    <td>图片</td>
					<th>酒店名称</th>
                    <th>操作</th>
                </tr>
                    #foreach($hotel in $hotelDOList)
                    <tr class="subCheckedTR">
                        <td><input class="subChecked" type="checkbox" value="$!{hotel.id}" hotelName="$!{hotel.name}"></td>
                        <td><img src="$!utilWebconfig.getTfsRootPath()$!hotel.logoUrl"></td>
                        <td>$!hotel.name</td>
                    </tr>
                    #end
            </table>
        </div>
     #*分页
    #parse("/page.vm")
    #pageStyle("themeListForm")
	*#
	<div class="clubBotBtn">
            <div class="btn-group">
                <button class="btn btn-success" type="button" id="btnSubmitHotel" >添加</button>
            </div>
            <div class="btn-group">
                <button class="btn">取消</button>
            </div>
   </div>
    </form>

<script>
	$('#btnSubmitHotel').click(function(){
	alert(1111)
 		openModalForForm(actionDefaultPath + '/B2C/resourceForSelect/selectHotel','选择酒店',function() {
			var sce = win.getItems();
			console.log(sce);
			$("#name").val(sce.name);
			$("#outId").val(sce.id);
			$("#picUrls").val(sce.logoUrl);
			return true;
		});
	})
</script>